# ADR 0006: Estado de misión explícito

Se persiste una máquina de etapas validada por `MissionSystem` en vez de flags dispersos en escenas. Esto evita saltos inválidos y permite añadir misiones sin rehacer el guardado.
